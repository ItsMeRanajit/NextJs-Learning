//if the folder name mentioned in the () ie (settings)
// then in the route to the files of settings we do not need to mention the folder name

// so the route will be localhost:3000/language
// we dont need to write /settings/
import React from 'react'

function language() {
  return (
    <div>language</div>
  )
}

export default language