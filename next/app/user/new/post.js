import React from 'react'

function post() {
  return (
    <div>post</div>
  )
}

export default post

// this is a nested router creation in next. the folder is in user. so the router for post will be like localhost:3000/user/new
//so for a nested route just create a nested folder/
//the route name is set as folder name not as file name